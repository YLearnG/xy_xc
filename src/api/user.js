import request from '@/utils/request'

export function login(data) {
  return request({
    url: '',
    methods: '',
    data
  })
}

export function logout(data) {
  return request({
    url: '',
    methods: '',
    data
  })
}
